import 'package:flutter/material.dart';

import 'config/constants/colors.dart';

ThemeData get appTheme => ThemeData(
      primaryColor: kColorPrimary,
      secondaryHeaderColor: kColorSecondary,
      fontFamily: 'Inter',
    );
