name: test_sumer
description: Sumer test.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_modular: ^5.0.3
  flutter_bloc: ^8.1.1
  get_it: ^7.2.0
  equatable: ^2.0.5
  dartz: ^0.10.1
  freezed: ^2.2.1
  freezed_annotation: ^2.2.0
  flutter_staggered_grid_view: ^0.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.3.2

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

